// import the required dependencies from React:
// createContext
// useState
// Create the Context
// Create a new context called CartContext using createContext()
// Set the default context value as an object with:
// isCartOpen: boolean (default false)
// setIsOpen: empty function
// Create the Provider Component
// Create a component called CartProvider that:
// Accepts children as a prop using destructuring
// Uses the useState hook to manage cart visibility
// Creates a value object containing the state and setter
// Returns the Context Provider wrapping the children